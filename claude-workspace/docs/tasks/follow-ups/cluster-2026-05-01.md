# Follow-up Cluster — 2026-05-01

**Status**: DONE — All 4 fixes implemented and tested.

---

## Fix 1 — Lot tooltip data path (TASK-042 F3)

**Worktree**: `clinic-cms-w2d`
**Approach chosen**: Option (a) — dedicated endpoint `GET /medicines/{id}/lots`

### Files touched (3)

| File | Change |
|------|--------|
| `app/modules/prescriptions/schemas/prescription_schemas.py` | Added `MedicineLotsResponse` schema (3 fields: medicine_id, lots, total) |
| `app/modules/prescriptions/api/routes.py` | Added `GET /medicines/{medicine_id}/lots` endpoint + import for `MedicineLotsResponse` |
| `tests/unit/test_medicine_lots_endpoint.py` | New: 8 unit tests covering LotInfo schema, FEFO ordering, HSD warning threshold |

### Implementation notes
- `get_lot_details()` already existed in `medicine_search_service.py` with FEFO ORDER BY + expiry_date NOT NULL filter.
- Endpoint lazy-returns lots for FE hover — no eager-load in search response.
- FE should call `GET /medicines/{id}/lots` on tooltip open and display HSD warning if `days_to_expiry < 30`.

### Test results: 8/8 PASSED

---

## Fix 2 — complete_visit_emr audit log (TASK-042 F2)

**Worktree**: `clinic-cms-w2d`

### Files touched (2)

| File | Change |
|------|--------|
| `app/modules/visits/services/emr_service.py` | Added `write_audit(action="visit.complete_emr", ...)` call before `log.info`; added `from app.core.audit import write_audit` import |
| `tests/unit/test_visit_complete_audit.py` | New: 7 unit tests (5 field-shape + 2 mock-based async tests verifying call count/kwargs) |

### Implementation notes
- `write_audit` called after `db.flush()` (within same transaction — atomic with state change).
- `old_data={"status": "IN_PROGRESS"}`, `new_data={"status": "AWAITING_PAYMENT", "doctor_id": str(doctor_id)}`.
- Consistent with billing flow audit pattern.

### Test results: 15/15 PASSED (includes Fix 1 tests run together)

---

## Fix 3 — BHYT settings PATCH audit log (TASK-034 F2)

**Worktree**: `clinic-cms-w2e`

### Files touched (2)

| File | Change |
|------|--------|
| `app/modules/bhyt/api/routes.py` | Added `from app.core.audit import write_audit`; capture `old_enabled`/`old_facility_code` before mutation; call `write_audit(action="settings.bhyt.toggle", entity_type="Clinic", entity_id=clinic_id, old_data={...}, new_data={...})` |
| `tests/integration/test_bhyt_settings_audit.py` | New: 8 tests (3 HTTP-level integration + 5 unit shape tests) |

### Implementation notes
- Old values captured before `clinic.bhyt_enabled = body.enabled` mutation.
- `write_audit` placed before `invalidate_feature_flag_cache` to ensure audit row committed with the mutation.
- `entity_id=clinic_id` (UUID context var) — consistent with admin module patterns.

### Test results: 8/8 PASSED

---

## Fix 4 — E2E specs clinic_code removal

**Worktree**: `clinic-cms-web` (new branch `feature/follow-up-e2e-clinic-code-removal` created from `feature/task-033-multi-clinic-fe`)

### Files touched (3)

| File | Lines changed | Change |
|------|--------------|--------|
| `e2e/smoke/auth-lockout.spec.ts` | 3 UI lines removed | Removed `expect(locator("#clinic_code..."))` visibility check + 2x `locator("#clinic_code").fill()` calls; kept API body `clinic_code: CLINIC` (lockout test sends raw API POST) |
| `e2e/smoke/patient-walkin.spec.ts` | 1 line | Replaced `#clinic_code` multi-selector with `#username` for form-render assertion |
| `e2e/helpers.ts` | 1 line + 1 comment | Removed `page.locator("#clinic_code").fill(clinicCode)` from `loginUI()` helper; added comment explaining removal |

### Implementation notes
- API test body `data: { clinic_code: CLINIC, ... }` in lockout test preserved — this is a direct API call simulating the old payload for lockout trigger; it is NOT a UI locator.
- `const CLINIC = DEMO.clinicCode` kept in auth-lockout.spec.ts for the API test.
- `helpers.ts` `apiLogin()` function still passes `clinic_code` in API body (intentional — server still accepts it even if FE no longer sends it from UI).
- Worktree: `clinic-cms-web-followup` (added at `E:/MyProject/clinic-cms-workspace/clinic-cms-web-followup`).

### E2E spec updates count: 4 clinic_code UI references removed across 3 files

---

## Summary

| Fix | Status | Tests | Files changed |
|-----|--------|-------|---------------|
| Fix 1 — Lot tooltip endpoint | DONE | 8/8 pass | 3 |
| Fix 2 — complete_visit_emr audit | DONE | 7/7 pass (in 15 total) | 2 |
| Fix 3 — BHYT settings audit | DONE | 8/8 pass | 2 |
| Fix 4 — E2E clinic_code removal | DONE | N/A (E2E specs) | 3 |

**Total files changed**: 10 across 3 worktrees + 1 new branch

**Blockers**: None
